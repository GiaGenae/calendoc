<% content_for :page_title, @appointment.date %> 

<section class="section">
	<div class="container">
		<nav class="level">
		  <!-- Left side -->
		  <div class="level-left">
		    <p class="level-item">
		        <strong>Actions</strong>
		    </p>
		  </div>
		  <!-- Right side -->
		  <div class="level-right">
		  	<p class="level-item">
		    	<%= link_to "Edit", edit_appointment_path(@appointment), class:"button" %>
		  	</p>
		  	<p class="level-item">
				<%= link_to "Delete", appointment_path(@appointment), method: :delete, data: { confirm: "Are you sure?" }, class:"button is-danger" %>
				</p>
		  </div>
		</nav>
		<hr/>

        <div class="date">
			<h1> <%= @appointment.date %> </h1>
		</div>
        <div class="appointment_type">
			<%= @appointment.appointment_type %>
		</div>
        <div class="time">
			<%= @appointment.time %>
		</div>
        <div class="notes"> Notes:
			<%= @appointment.notes %>
		</div>
	</div>
</section>

